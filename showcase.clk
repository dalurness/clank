
main : () -> <io, async> () =
  let file = fs.open("package.json")
  let contents = fs.read(file)
  fs.close(file)

  let maybeval = json.get(contents, "name")
  match maybeval {
    Some(val) => print(val)
    None => print("idk wtf this is")
  }
  

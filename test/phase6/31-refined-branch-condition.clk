# Test: branch condition refines variable — x > 0 in then branch satisfies positive refinement
# Expected output:
# 5

positive : (x: Int{v > 0}) -> <> Int = x

safe-call : (x: Int) -> <> Int
  = if gt(x, 0) then positive(x) else 0

main : () -> <io> () = print(show(safe-call(5)))

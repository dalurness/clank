# Test: else branch gets negated condition — x <= 0 known in else branch
# Expected output:
# -3

non-positive : (x: Int{v <= 0}) -> <> Int = x

check : (x: Int) -> <> Int
  = if gt(x, 0) then 1 else non-positive(x)

main : () -> <io> () = print(show(check(-3)))

# Test: per-branch return refinement fails when a branch returns value violating predicate
# Expected: error

bad-return : (x: Int) -> <> Int{v > 0} =
  if gt(x, 0) then x else 0

main : () -> <io> () = print(show(bad-return(5)))

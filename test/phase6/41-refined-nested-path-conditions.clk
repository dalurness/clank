# Test: nested if branches accumulate path conditions — x > 0 && x < 100
# Expected output:
# 50

bounded : (x: Int{v > 0 && v < 100}) -> <> Int = x

check : (x: Int) -> <> Int
  = if gt(x, 0) then (if lt(x, 100) then bounded(x) else 1) else 1

main : () -> <io> () = print(show(check(50)))

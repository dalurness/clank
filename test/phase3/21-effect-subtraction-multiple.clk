# Test: subtracting multiple effects at once
# <io, exn, log \ exn, log> should leave only io

effect log {
  info : Str -> ()
}

effect exn {
  raise : Str -> ()
}

main : () -> <io, exn, log \ exn, log> () =
  print("multi-subtract works")

# Expected output:
# multi-subtract works

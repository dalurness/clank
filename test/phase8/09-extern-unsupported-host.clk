# Test: extern with unsupported host (python) should produce runtime error
# E804: host not supported

extern "json" py-dumps : (obj: Str) -> <io> Str
  where host = "python", symbol = "dumps"

main : () -> <io> () =
  print(py-dumps("hello"))

# Expected: error

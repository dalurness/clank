# Test: extern with non-existent module should produce runtime error
# E800: failed to load extern module

extern "nonexistent-module-xyz" bad-fn : (x: Str) -> <io> Str
  where host = "js"

main : () -> <io> () =
  print(bad-fn("hello"))

# Expected: error

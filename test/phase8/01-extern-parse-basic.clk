# Test: basic extern declaration parsing
# Declares and calls an extern JS function via node:path

extern "node:path" basename : (path: Str) -> <io> Str
  where host = "js"

main : () -> <io> () =
  print(basename("/home/user/file.txt"))

# Expected output:
# file.txt

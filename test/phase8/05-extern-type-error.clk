# Test: extern with user-defined effect should produce type error
# E-FFI-4: foreign functions cannot declare user-defined effects

effect MyEffect {
  tick : () -> ()
}

extern "mylib" bad-fn : (x: Int) -> <MyEffect> Int
  where host = "js"

main : () -> <io> () =
  print("should not reach here")

# Expected: error

# Test: extern with symbol rename
# Uses where symbol to map to a different function name

extern "node:path" dir-name : (path: Str) -> <io> Str
  where host = "js", symbol = "dirname"

main : () -> <io> () =
  print(dir-name("/home/user/file.txt"))

# Expected output:
# /home/user
